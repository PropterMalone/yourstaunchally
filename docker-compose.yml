services:
  engine:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/data/yourfriend.db
      - PYTHON_PATH=/app/.venv/bin/python3
    volumes:
      - ./data:/data
